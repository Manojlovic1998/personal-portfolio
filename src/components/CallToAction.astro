---
interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a
	href={href}
	class="rounded-[3px] flex flex-row gap-1 px-5 py-3 bg-button-primary w-fit text-skin-inverted font-semibold items-center"
	><slot /></a
>
